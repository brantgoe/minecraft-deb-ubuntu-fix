---
name: A dependency wasn't resolved
about: The script stopped with an ERR about a dependency it couldn't satisfy or map
title: "[deps] "
labels: dependency
---

<!--
Thanks for helping! The two things below are what let us add a fix.
Paste the requested output between the ``` fences.
-->

**Your Ubuntu version** — run `cat /etc/os-release` and paste the output:

```
(paste here)
```

**The script output** — paste the full run, especially the red `ERR` line:

```
(paste here)
```

**Which dependency did it name?** (e.g. `libsomething2.0-0`)

**Minecraft launcher version**, if you know it (from https://www.minecraft.net/download):
